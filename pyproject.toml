[project]
name = "project-x"
version = "0.0.0"
description = "AI-first IR core with verifier and projections"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
