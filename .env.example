# Copy to `.env` in the repo root (never commit `.env`).
# OpenAI: https://platform.openai.com/api-keys

OPENAI_API_KEY=
# Optional override:
# OPENAI_IR_MODEL=gpt-4o-mini

# Web console (`project-x-console` / `python -m webui`):
# PROJECT_X_HOST=127.0.0.1
# PROJECT_X_PORT=8000
