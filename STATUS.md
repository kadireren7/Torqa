# Project-X — current maturity

**What is real:** Canonical IR (v1.4), Python + Rust verification/execution, orchestrator, multi-target codegen, golden tests, diagnostics codes, web console (Monaco), CLI, patch preview, system health report, CI, bundle envelope checks, 1.3→1.4 migration, `.pxir` surface subset, optional `library_refs`, third-party projection env hook.

**What is partial:** Rust toolchain on all dev machines, execution trace richness, full editor session/undo, enterprise security, deep multi-domain projections.

**What is experimental:** Self-evolution modules, aggressive legacy removal.

**Next milestone (recommended):** Harden website path + generation quality gates; expand golden tests around `check` and parity; optional self-hosted Monaco assets.
