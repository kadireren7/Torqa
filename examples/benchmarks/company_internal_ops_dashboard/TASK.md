# Benchmark: internal ops metrics command center (frozen wording)

Build an **executive / ops dashboard** for comparing **metrics** across **dimensions** (region, product line, team) with:

- **Filters**: report scope, time range, dimension slice.
- **Drilldown**: entity-level exception queue id.
- **Comparison**: prior window vs current window.
- **Exports**: export job id for scheduled CSV / PDF drops.
- **Anomaly** callouts when KPIs breach thresholds.

The experience should read as an **internal analytics console** (dense controls, not a public marketing site).

_Version: P128 benchmark seed — keep this file stable for token comparisons._
