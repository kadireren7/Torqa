# MinimalLogin — demo webapp

This folder is produced by **TORQA** projection (Vite + React). Use it as a local preview baseline; API and auth are not wired here.

## Run locally

```bash
npm install
npm run dev
```

Open the URL Vite prints (often http://localhost:5173).
